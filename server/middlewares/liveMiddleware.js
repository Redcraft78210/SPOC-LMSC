// verifier que l'utilisatteur avec authmiddleware qu'il est bien acces au live

// récuperer la classe de l'eleve (classeid) et utiliser la methode livecontroller.getlivebyclass et
// vérifie si dans la reponse , il y esty l'id du live demander dans la requete get

